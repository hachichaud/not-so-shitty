.column(
  ng-repeat="type in listTypes"
) 
  span.list-type Select {{ type }} column
  select(
    ng-options="column.id as column.name for column in boardColumns"
    ng-model="columnIds[type]"
  )
  span.required(ng-if="type == 'done'") *required
